import { faker } from "@faker-js/faker";
import {
  Page,
  expect,
  test,
  chromium,
  Browser,
  BrowserContext,
} from "@playwright/test";
import {
  webUrl,
  webUrl1,
  webUrl2,
  webUrl3,
  webUrl4,
  webUrl5,
  webUrl6,
  webUrl7,
  webUrl8,
  webUrl9,
  webUrl10,
  webUrl11,
  webUrl12,
  webUrl13,
  webUrl14,
  webUrl15,
  webUrl16,
  webUrl17,
  webUrl18,
  webUrl19,
  webUrl20,
  webUrl21,
  webUrl22,
  webUrl23,
  webUrl24,
  webUrl25,
  webUrl26,
  webUrl27,
  webUrl28,
  webUrl29,
} from "../config/config";

test.describe("Create Account", () => {
    test('m2d1 create account', async ({ page }) => {
        await page.goto(webUrl);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d2 create account', async ({ page }) => {
        await page.goto(webUrl1);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d3 create account', async ({ page }) => {
        await page.goto(webUrl2);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d4 create account', async ({ page }) => {
        await page.goto(webUrl3);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d5 create account', async ({ page }) => {
        await page.goto(webUrl4);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d6 create account', async ({ page }) => {
        await page.goto(webUrl5);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d7 create account', async ({ page }) => {
        await page.goto(webUrl6);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d8 create account', async ({ page }) => {
        await page.goto(webUrl7);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d9 create account', async ({ page }) => {
        await page.goto(webUrl8);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d10 create account', async ({ page }) => {
        await page.goto(webUrl9);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d11 create account', async ({ page }) => {
        await page.goto(webUrl10);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d12 create account', async ({ page }) => {
        await page.goto(webUrl11);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d13 create account', async ({ page }) => {
        await page.goto(webUrl12);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d14 create account', async ({ page }) => {
        await page.goto(webUrl13);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d15 create account', async ({ page }) => {
        await page.goto(webUrl14);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d16 create account', async ({ page }) => {
        await page.goto(webUrl15);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d17 create account', async ({ page }) => {
        await page.goto(webUrl16);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d18 create account', async ({ page }) => {
        await page.goto(webUrl17);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d19 create account', async ({ page }) => {
        await page.goto(webUrl18);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d20 create account', async ({ page }) => {
        await page.goto(webUrl19);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d21 create account', async ({ page }) => {
        await page.goto(webUrl20);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d22 create account', async ({ page }) => {
        await page.goto(webUrl21);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d23 create account', async ({ page }) => {
        await page.goto(webUrl22);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d24 create account', async ({ page }) => {
        await page.goto(webUrl23);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d25 create account', async ({ page }) => {
        await page.goto(webUrl24);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d26 create account', async ({ page }) => {
        await page.goto(webUrl25);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d27 create account', async ({ page }) => {
        await page.goto(webUrl26);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d28 create account', async ({ page }) => {
        await page.goto(webUrl27);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d29 create account', async ({ page }) => {
        await page.goto(webUrl28);
        // Add your test steps here
        await page.close();
      });
      
      test('m2d30 create account', async ({ page }) => {
        await page.goto(webUrl29);
        // Add your test steps here
        await page.close();
      });
});
